# ── Required ────────────────────────────────────────────────────────────────
# Invitation code users must enter before they can register via the web portal
# Leave empty to allow open registration (not recommended for production)
INVITE_CODE=your-invite-code-here

# ── Gateway ──────────────────────────────────────────────────────────────────
# Secret used to sign JWTs for web portal sessions — generate with: openssl rand -hex 32
JWT_SECRET=changeme-set-to-a-random-secret

# ── Infrastructure ───────────────────────────────────────────────────────────
# Hostname or IP of the Docker host (used in Gitea/Woodpecker external URLs)
DS1_HOST=ds1
# Public domain for live app URLs — apps are served at {app-name}.{APP_DOMAIN}
# Route *.APP_DOMAIN → ds1:80 (Traefik) via Cloudflare Tunnel or DNS
APP_DOMAIN=apps.yourdomain.com
POSTGRES_PASSWORD=changeme

# ── LLM ─────────────────────────────────────────────────────────────────────
OLLAMA_MODEL=llama3.1:8b

# ── Gitea ────────────────────────────────────────────────────────────────────
# Admin user created automatically on first start by the gitea-init service
GITEA_ADMIN_USER=platform
GITEA_ADMIN_PASS=changeme
GITEA_ADMIN_EMAIL=admin@platform.local

# ── Woodpecker CI ─────────────────────────────────────────────────────────────
# OAuth2 app credentials — see README step 4 (one-time manual setup in Gitea UI)
WOODPECKER_GITEA_CLIENT=
WOODPECKER_GITEA_SECRET=
# Shared secret between woodpecker-server and woodpecker-agent
WOODPECKER_AGENT_SECRET=changeme
# Running L2 behavioral tests
BEHAVIORAL_LLM_PROVIDER=anthropic
INTAKE_LLM_MODEL=claude-haiku-4-5
ANTHROPIC_API_KEY=your-anthropic-api-key-here