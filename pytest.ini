[pytest]
pythonpath = agents/_lib agents/intake
testpaths = tests
markers =
    contract: Fast contract tests — no LLM required. Run on every PR.
    behavioral: Behavioral tests — require a real LLM. Skipped if BEHAVIORAL_LLM_PROVIDER is unset.
    quality: LLM-as-judge quality scoring — slow, run on schedule only.
