apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    access: proxy
    url: postgres:5432
    database: platform
    user: platform
    isDefault: false
    jsonData:
      sslmode: disable
      postgresVersion: 1400
    secureJsonData:
      password: ${POSTGRES_PASSWORD:-changeme}
